rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // ===============================================================
    // Helper Functions
    // ===============================================================
    function isAuthenticated() {
      return request.auth != null;
    }
    
    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    function isStaticSuperAdmin() {
      return isAuthenticated() && (
        ('email' in request.auth.token && 
         (request.auth.token.email == 'exportacoes.extras@gmail.com' || request.auth.token.email == 'omilionario.extra@gmail.com')) ||
        (exists(/databases/$(database)/documents/usuarios/$(request.auth.uid)) && 
         'email' in get(/databases/$(database)/documents/usuarios/$(request.auth.uid)).data &&
         (get(/databases/$(database)/documents/usuarios/$(request.auth.uid)).data.email == 'exportacoes.extras@gmail.com' || 
          get(/databases/$(database)/documents/usuarios/$(request.auth.uid)).data.email == 'omilionario.extra@gmail.com')) ||
        (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && 
         'email' in get(/databases/$(database)/documents/users/$(request.auth.uid)).data &&
         (get(/databases/$(database)/documents/users/$(request.auth.uid)).data.email == 'exportacoes.extras@gmail.com' || 
          get(/databases/$(database)/documents/users/$(request.auth.uid)).data.email == 'omilionario.extra@gmail.com'))
      );
    }

    function isAdmin() {
      return isStaticSuperAdmin() || (
        isAuthenticated() && (
          (exists(/databases/$(database)/documents/usuarios/$(request.auth.uid)) && 
           'role' in get(/databases/$(database)/documents/usuarios/$(request.auth.uid)).data &&
           get(/databases/$(database)/documents/usuarios/$(request.auth.uid)).data.role == 'admin') ||
          (exists(/databases/$(database)/documents/users/$(request.auth.uid)) && 
           'role' in get(/databases/$(database)/documents/users/$(request.auth.uid)).data &&
           get(/databases/$(database)/documents/users/$(request.auth.uid)).data.role == 'admin')
        )
      );
    }

    function isPartner(userId) {
      return isAuthenticated() && 
        'email' in request.auth.token && (
          (exists(/databases/$(database)/documents/usuarios/$(userId)) && 
           'partnerEmail' in get(/databases/$(database)/documents/usuarios/$(userId)).data &&
           get(/databases/$(database)/documents/usuarios/$(userId)).data.partnerEmail == request.auth.token.email) ||
          (exists(/databases/$(database)/documents/users/$(userId)) && 
           'partnerEmail' in get(/databases/$(database)/documents/users/$(userId)).data &&
           get(/databases/$(database)/documents/users/$(userId)).data.partnerEmail == request.auth.token.email)
        );
    }

    function canReadData() {
      return isAuthenticated() && (
        (resource != null && 'userId' in resource.data && resource.data.userId == request.auth.uid) || 
        isAdmin() || 
        ('email' in request.auth.token && resource != null && 'userId' in resource.data && (
          (exists(/databases/$(database)/documents/usuarios/$(resource.data.userId)) && 
           'partnerEmail' in get(/databases/$(database)/documents/usuarios/$(resource.data.userId)).data &&
           get(/databases/$(database)/documents/usuarios/$(resource.data.userId)).data.partnerEmail == request.auth.token.email) ||
          (exists(/databases/$(database)/documents/users/$(resource.data.userId)) && 
           'partnerEmail' in get(/databases/$(database)/documents/users/$(resource.data.userId)).data &&
           get(/databases/$(database)/documents/users/$(resource.data.userId)).data.partnerEmail == request.auth.token.email)
        ))
      );
    }

    // ===============================================================
    // Collection Group Rules
    // ===============================================================
    match /{path=**}/comments/{commentId} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated();
    }

    match /{path=**}/messages/{messageId} {
      allow read: if isAuthenticated();
      allow write: if isAuthenticated();
    }

    // ===============================================================
    // Collection Rules
    // ===============================================================

    // User Profiles / Billing
    match /user_profiles/{profileId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow update: if isAuthenticated() && (resource.data.userId == request.auth.uid || isAdmin());
      allow delete: if isStaticSuperAdmin();
    }

    // Settings
    match /settings/global {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Referrals
    match /referrals/{referralId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && (request.resource.data.referredId == request.auth.uid || isAdmin());
      allow update, delete: if isAdmin();
    }

    // Affiliate Payouts
    match /affiliate_payouts/{payoutId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow update, delete: if isAdmin();
    }

    // Payments
    match /payments/{paymentId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow update, delete: if isAdmin();
    }

    // Community
    match /community_posts/{postId} {
      allow read: if true;
      allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow update: if isAuthenticated() && (
        resource.data.userId == request.auth.uid || 
        isAdmin() || 
        request.resource.data.diff(resource.data).affectedKeys().hasOnly(['likesCount', 'commentsCount'])
      );
      allow delete: if isAuthenticated() && (resource.data.userId == request.auth.uid || isAdmin());

      match /comments/{commentId} {
        allow read: if true;
        allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
        allow update: if isAuthenticated() && resource.data.userId == request.auth.uid;
        allow delete: if isAuthenticated() && (
          resource.data.userId == request.auth.uid || 
          get(/databases/$(database)/documents/community_posts/$(postId)).data.userId == request.auth.uid || 
          isAdmin()
        );
      }

      match /likes/{userId} {
        allow read: if true;
        allow write: if isAuthenticated() && request.auth.uid == userId;
      }
    }

    // Broadcasts
    match /broadcasts/{broadcastId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // User Profiles - Both paths for compatibility
    match /users/{userId} {
      allow read, write: if isAuthenticated();

      match /friends/{friendId} {
        allow read, write: if isAuthenticated();
      }
      match /distancing/{distanceId} {
        allow read, write: if isAuthenticated();
      }
      match /blocks/{blockId} {
        allow read, write: if isAuthenticated();
      }
    }

    match /usuarios/{userId} {
      allow read: if true;
      allow create: if isAuthenticated() && request.auth.uid == userId;
      allow update: if isOwner(userId) || isAdmin() || isPartner(userId);
      allow delete: if isAdmin();

      // Subcollections
      match /trades/{tradeId} {
        allow read, write: if isAuthenticated();
      }
      match /accounts/{accountId} {
        allow read, write: if isAuthenticated();
      }
      match /followers/{followerId} {
        allow read, write: if isAuthenticated();
      }
      match /following/{followingId} {
        allow read, write: if isAuthenticated();
      }
    }

    // Legacy Accounts & Trades
    match /accounts/{accountId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow update, delete: if canReadData();
    }

    match /trades/{tradeId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow update, delete: if canReadData();
    }

    // Withdrawals
    match /withdrawals/{withdrawalId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && request.resource.data.userId == request.auth.uid;
      allow update, delete: if canReadData();
    }

    // Psychology Notes
    match /psychology_notes/{noteId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && (request.resource.data.userId == request.auth.uid || isAdmin() || isPartner(request.resource.data.userId));
      allow update, delete: if canReadData();
    }

    // Coupons
    match /coupons/{couponId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Planning
    match /planning/{entryId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && (request.resource.data.userId == request.auth.uid || isAdmin() || isPartner(request.resource.data.userId));
      allow update, delete: if canReadData();
    }

    // Objectives
    match /objectives/{objectiveId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated() && (request.resource.data.userId == request.auth.uid || isAdmin() || isPartner(request.resource.data.userId));
      allow update, delete: if canReadData();
    }

    // Chat System
    match /chats/{chatId} {
      allow read, write: if isAuthenticated();

      match /messages/{messageId} {
        allow read, write: if isAuthenticated();
      }
    }

    // Room Invites System
    match /room_invites/{inviteId} {
      allow read, write: if isAuthenticated();
    }

    // Friend Requests System
    match /friend_requests/{requestId} {
      allow read, write: if isAuthenticated();
    }
  }
}
